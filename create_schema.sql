CREATE TABLE C##PROJETO.processos (
    id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    tipo_de_processo NUMBER NOT NULL,
    numero VARCHAR2(50) NOT NULL,
    data_entrada DATE NOT NULL,
    valor_recurso NUMBER(15, 2) NOT NULL,
    objetivo VARCHAR2(255) NOT NULL
) TABLESPACE USERS;

CREATE TABLE C##PROJETO.tipos_de_processo (
    id NUMBER  GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    nome VARCHAR2(100) NOT NULL
) TABLESPACE USERS;


ALTER TABLE C##PROJETO.processos
ADD CONSTRAINT fk_tipo_de_processo
FOREIGN KEY (tipo_de_processo)
REFERENCES C##PROJETO.tipos_de_processo (id);

 


 

